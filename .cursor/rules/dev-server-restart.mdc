---
description: 修改需重启生效时由助手直接重启开发服务，勿仅文字提醒用户自行重启
globs: "**/*"
---

# 开发服务重启

当本次改动满足任一情况时，**在任务完成后直接在本机执行重启**（勿只写「请重启」）：

- 修改了项目根目录 `.env` 或环境变量相关说明且会影响运行中的进程
- 修改了服务端入口、`server/_core/index.ts`、Vite 配置等需重载才能生效的文件

**命令**（在项目根目录，先释放 3000 端口与 tsx watch）：

```bash
(lsof -ti:3000 | xargs kill -9 2>/dev/null || true); pkill -f "tsx watch server/_core/index.ts" 2>/dev/null || true; sleep 1; cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)" && npm run dev
```

使用 `block_until_ms: 0` 后台运行即可。若用户未使用 3000 端口，以实际端口为准。
