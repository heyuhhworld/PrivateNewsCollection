---
description: 需要执行命令或重启开发服务时由助手直接执行，勿让用户自行运行或重启
globs: "**/*"
---

# 命令执行与开发服务

## 总原则

- **不要**在回复里写「请你执行 `pnpm …`」「请重启 dev」等让用户自己去做的说明。
- 需要跑脚本、数据库对齐、类型检查、探活等时，**在对话内直接用工具执行**（项目根目录）。
- 需要让改动在本地跑起来时，**直接重启开发服务**（见下文），而不是只提醒用户重启。

## 何时直接重启 dev

当本次改动满足任一情况时，**在任务完成后直接在本机重启**：

- 修改了项目根目录 `.env` 或会影响运行中进程的环境变量相关代码/说明
- 修改了服务端入口、`server/_core/index.ts`、Vite 配置等需重载才能生效的文件

**推荐**（与仓库脚本一致，含杀端口、库表对齐、再起 dev）：在项目根执行 `pnpm run dev:restart`，或对当前环境用等价流程（先释放 `PORT` 默认 3000，再起 `pnpm dev`）。后台运行可用 `block_until_ms: 0`。

## 常见应直接执行的操作（勿转嫁用户）

- 结构/SQL 变更后：`pnpm run db:ensure-schema`
- 验证类型：`npx tsc --noEmit`
- 探活：`pnpm run dev:verify` / `pnpm run dev:check`（按需）

若命令失败，在对话里说明错误与已尝试的修复；**仍不要**只丢一句「请你在终端执行」作为唯一收尾。
